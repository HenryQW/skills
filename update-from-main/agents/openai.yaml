interface:
  display_name: "Update From Main"
  short_description: "Update current worktree from origin/main"
  default_prompt: "Use $update-from-main to fetch origin/main, update current worktree branch from its exact ref, stash local changes, and resolve any conflicts."
