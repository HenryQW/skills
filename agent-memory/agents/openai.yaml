interface:
  display_name: "Agent Memory"
  short_description: "Use and distill project agent memory"
  default_prompt: "Use $agent-memory load to load approved project memory or $agent-memory distill to preview durable decision candidates; pass --setup only for explicit bootstrap."
