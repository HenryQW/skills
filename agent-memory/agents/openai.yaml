interface:
  display_name: "Agent Memory"
  short_description: "Use and distill project agent memory"
  default_prompt: "Use $agent-memory to load, capture, or distill deterministic project memory; pass --setup only for explicit bootstrap."
