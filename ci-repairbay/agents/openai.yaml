interface:
  display_name: "CI Repairbay"
  short_description: "Inspect or fix GitHub Actions checks"
  icon_small: "./assets/github-small.svg"
  icon_large: "./assets/github.png"
  default_prompt: "Use $ci-repairbay to inspect the failing GitHub Actions checks on this PR and report the root cause. If I explicitly ask to fix them, apply the smallest scoped local fix and validate it."
