interface:
  display_name: "Issue Blueprint"
  short_description: "Turn grilled specs into issue graphs"
  default_prompt: "Use $issue-blueprint. Lock no-code issue-only intent first, run at most one compact adversarial review unless correctness blockers remain, then publish the dependency-aware issue graph."
