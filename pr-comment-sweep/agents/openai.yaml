interface:
  display_name: "PR Comment Sweep"
  short_description: "Assess and fix actionable PR comments"
  default_prompt: "Use $pr-comment-sweep as standalone workflow to assess and fix current PR feedback, validate, commit, push, and resolve addressed threads without approval pauses unless blocked."
