interface:
  display_name: "Shipyard"
  short_description: "Orchestrate parent issues from the deterministic integration branch"
  default_prompt: "Use $shipyard with a parent issue number or URL and execute it unless I explicitly ask for inspect-only output."
