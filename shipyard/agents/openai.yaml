interface:
  display_name: "Shipyard"
  short_description: "Orchestrate parent issues from the deterministic integration branch"
  default_prompt: "Use $shipyard with a parent issue number or URL; reconcile to the parent-derived integration branch, then run dependency-wave integration worktrees."
