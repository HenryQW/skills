interface:
  display_name: "Grill to Issues"
  short_description: "Turn grilled specs into issue graphs"
  default_prompt: "Use $grill-to-issues. Prefer existing approved specs, run at most one compact adversarial review unless correctness blockers remain, then publish the dependency-aware issue graph."
